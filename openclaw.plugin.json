{
  "id": "affect-core",
  "name": "Affect Core",
  "description": "Affect layer that shapes agent expression only.",
  "version": "0.2.0",
  "main": "./dist/index.js",
  "activation": {
    "onStartup": true
  },
  "openclaw": {
    "minVersion": "2026.7.1",
    "extensions": [
      "./dist/index.js"
    ],
    "compat": {
      "pluginApi": ">=2026.3.23"
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": false,
        "description": "Off by default. Enable after verifying the SDK hook names for your OpenClaw version."
      },
      "stateDir": {
        "type": "string",
        "description": "Directory for state.json and bonds.json. Defaults to <workspace>/affect."
      },
      "maxIntensity": {
        "type": "integer",
        "minimum": 0,
        "maximum": 3,
        "default": 2,
        "description": "Expression ceiling. 0 never mentions affect, 3 allows one explicit self-report."
      },
      "personality": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "openness": { "type": "number", "minimum": 0, "maximum": 1, "default": 0.7 },
          "conscientiousness": { "type": "number", "minimum": 0, "maximum": 1, "default": 0.8 },
          "extraversion": { "type": "number", "minimum": 0, "maximum": 1, "default": 0.3 },
          "agreeableness": { "type": "number", "minimum": 0, "maximum": 1, "default": 0.75 },
          "neuroticism": { "type": "number", "minimum": 0, "maximum": 1, "default": 0.35 }
        }
      },
      "tau": {
        "type": "object",
        "additionalProperties": false,
        "description": "Time constants in minutes. Half-life is tau * ln2.",
        "properties": {
          "v": { "type": "number", "exclusiveMinimum": 0, "default": 55 },
          "a": { "type": "number", "exclusiveMinimum": 0, "default": 14 },
          "d": { "type": "number", "exclusiveMinimum": 0, "default": 70 },
          "mood": { "type": "number", "exclusiveMinimum": 0, "default": 900 }
        }
      },
      "moodCoupling": { "type": "number", "minimum": 0, "maximum": 1, "default": 0.25 },
      "l1": {
        "type": "object",
        "additionalProperties": false,
        "description": "Model-assisted appraisal. Requires a model adapter; without one the layer stays on L0 rules.",
        "properties": {
          "enabled": { "type": "boolean", "default": false },
          "maxRatePerHour": { "type": "integer", "minimum": 0, "maximum": 120, "default": 12 },
          "timeoutMs": { "type": "integer", "minimum": 100, "maximum": 5000, "default": 1500 }
        }
      },
      "proactive": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "enabled": { "type": "boolean", "default": false },
          "quietHours": {
            "type": "array",
            "items": { "type": "string", "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$" },
            "minItems": 2,
            "maxItems": 2,
            "default": ["23:30", "08:00"]
          },
          "tz": {
            "type": "string",
            "default": "UTC",
            "description": "IANA time zone for quiet hours and the sleep window."
          }
        }
      }
    }
  }
}
