# 参与 affect-core

先说最重要的一条：**改动力学的 PR 必须附带一条会因该改动而失败的测试。** 没有测试的行为改动只是一个主张，不是一次修复。这个项目的正确性无法靠「感觉像不像有情绪」判断，只能靠不变量。

## 环境

需要 Node 22.6+ 和 pnpm。

```bash
pnpm install
pnpm run check      # 类型检查 + 27 项测试
pnpm run test:watch
pnpm run build
```

## 项目约定

- **`dynamics.ts` 必须保持纯函数、零 I/O。** 它能用假时间戳跑几千步，这是全部自动化验证的前提。要读时间就把 `now` 当参数传进去。
- **SDK 名称只能出现在 `src/index.ts`。** 内核不许 import 任何 OpenClaw 类型。签名猜错时，影响面必须限制在一个文件。
- **注入片段里不许出现裸数值。** 给模型演出指令，不给状态转储，否则它会开始朗读自己的参数。
- **每条失败路径都要 fail-open。** 情感层是装饰性子系统，它把主功能拖挂比它自己算错严重一个量级。
- **TypeScript 严格模式不许放宽。** `noUncheckedIndexedAccess` 和 `exactOptionalPropertyTypes` 都是开的，别为了图快关掉。
- 不做无关的格式化、重命名和重构。改动保持最小且可分离。

## 调参顺序

如果你在调数值：先定 baseline，再定时间常数 τ，最后才碰事件增量表。反过来调必陷进去。

注意 **τ 是时间常数，不是半衰期**（半衰期 = τ·ln2 ≈ 0.69τ）。

## 提 PR 之前

1. `pnpm run check` 全绿。
2. 新增或修改的行为有对应断言。
3. 如果动了不变量的语义，在 PR 描述里说清原本保护的是什么、为什么现在不需要了。**用削弱不变量的方式修 bug 是回归，不是修复。**
4. 如果动了 `src/index.ts` 的钩子名或字段，说明你是在哪个 OpenClaw 版本上核对的。

## 提 issue

情感行为类的 issue 请附上：配置片段（去掉密钥）、`/mood` 输出、触发的事件序列，以及你期望的表现。「它感觉不对」很难查，「连点五次夸奖后 mood 没动」一眼就能定位。

## 行为准则

参与本项目即表示同意遵守 [CODE_OF_CONDUCT.md](./CODE_OF_CONDUCT.md)。
