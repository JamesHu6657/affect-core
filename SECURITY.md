# 安全策略

安全修复只针对 `main`。

请勿用公开 issue 报告安全问题，走 [私密报告通道](https://github.com/JamesHu6657/affect-core/security/advisories/new)。带上影响的 commit、复现步骤和实际影响。

## 风险面

情感层不做网络请求、不执行代码、不接触凭据。值得注意的是这几处：

- **提示注入**。`express.ts` 会把生成的文本追加进系统提示。注入内容全部由本地状态派生的固定模板产生，用户消息不会被原样拼进去。任何能让用户输入进入注入片段的路径都算安全问题
- **状态文件**。`state.json` 和 `bonds.json` 存在 workspace 里，包含按用户 ID 累积的关系数值。不存消息原文，但足以推断交互历史，按普通用户数据对待，注意目录权限
- **L1 模型调用**。启用后会把消息内容送给你自己配置的模型适配器。是否离开本机取决于你的 OpenClaw 配置。默认关闭
- **拒绝服务**。增量和调用频率都有上限（单事件封顶、习惯化、令牌桶、48 小时 dt 钳制）。能绕过这些界限、把状态推向无界或打爆心跳的输入，请按漏洞报告

## 不算漏洞

情绪表现「不真实」或「不合适」是调参问题，走普通 issue。自己把 `maxIntensity` 或 `l1` 配得过激导致的行为也一样。
