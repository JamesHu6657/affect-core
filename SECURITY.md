# 安全策略

## 支持版本

本项目处于 `0.2.x` 早期阶段，安全修复只针对 `main` 分支。

## 报告漏洞

**请不要用公开 issue 报告安全问题。** 使用 GitHub 的私密报告通道：
[Security → Report a vulnerability](https://github.com/JamesHu6657/affect-core/security/advisories/new)。

请尽量包含：影响版本或 commit、复现步骤、实际影响，以及你认为的修复方向。会在 7 天内回复。

## 这个项目的风险面

情感层本身不做网络请求、不执行代码、不接触凭据。值得关注的是这几处：

- **提示注入面**：`express.ts` 把生成的文本追加进系统提示。所有注入内容都由本地状态派生的固定模板产生，用户消息内容不会被原样拼接进去。任何能让用户输入进入注入片段的路径都是安全问题。
- **状态文件**：`state.json` 与 `bonds.json` 存在 agent workspace 里，包含按用户 ID 累积的关系数值。它不存储消息原文，但足以推断交互历史。请按普通用户数据对待，并注意目录权限。
- **L1 模型调用**：启用后会把消息内容送给你自己配置的模型适配器。是否离开本机取决于你的 OpenClaw 配置，不由本插件决定。默认关闭。
- **拒绝服务**：所有增量与调用频率都有上限（单事件封顶、习惯化、令牌桶、48 小时 dt 钳制）。发现任何可以绕过这些界限、把状态推向无界或让心跳打爆资源的输入，请按漏洞报告。

## 明确不属于漏洞的情形

- 代理表达出的情绪「不真实」或「不合适」。这是调参问题，走普通 issue。
- 用户自己把 `maxIntensity` 或 `l1` 配得过激导致的行为。
- 依赖库的公开 CVE，但在本项目中不可达。
