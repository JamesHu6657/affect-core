## 改了什么，为什么

<!-- 如果修的是缺陷，说明在什么条件下会出问题。 -->

## 检查

- [ ] `pnpm run check` 通过
- [ ] 行为改动有一条改动前会失败的测试
- [ ] `dynamics.ts` 仍是纯函数、零 I/O
- [ ] OpenClaw SDK 名称没扩散到 `src/index.ts` 之外
- [ ] 注入片段里没有裸数值

如果削弱或删掉了任何不变量，说明原本保护的是什么。改了 `index.ts` 的钩子名或字段的话，说明在哪个 OpenClaw 版本上核对的。
